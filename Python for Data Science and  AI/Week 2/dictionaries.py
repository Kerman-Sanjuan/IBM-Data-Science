#Type of collection
#Instead of using index, they use "keys"
#They keys cannot be duplicated and are unique.
Dictionary = {
    "nombres":["kerman","janire"],
    "edad": [1,2],
    "donde_viven":["Algorta", "Plen"]
}

print(Dictionary['edad'])
